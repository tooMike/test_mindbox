from setuptools import setup, find_packages

setup(
    name='geometry',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Mikhail Beliaev',
    description='A library for calculating areas of different shapes',
    long_description=open('README.md').read(),
)
